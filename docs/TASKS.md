# Tasks

Seed for the shared Google Doc (copy it over, or track here and tick boxes in PRs — your call). IDs match [task-dependencies.md](task-dependencies.md); `E*` rows are clock events that live only here. Everything traces to a decision in [DESIGN.md](DESIGN.md) §2; where a row needs something undecided, it says so and is a decision row (`D*`).

**Deadline Sat 23:59 · submit by 22:30 · feature freeze 19:00.**
**Sat 08:00 — reassessed against the repo (branch `night/all`; what the code is: [CODEBASE.md](CODEBASE.md)).** Friday's target is met: template + day flow + three scenes (day_template, day_panic, platforming_day) + a transition + intro + reunion run end to end, bot-proven (`tools/smoke_test.sh`), web build boots. **Not yet:** on `main` (PR #16 + `night/*` unmerged — N3), on itch (P1 unconfirmed), an end card (N1), HUD win-lights (T7), day cards (C1), days 3–5 (C3b–d). ✔ = evidence in the repo · ◐ = partial · *(reassessed)* = reworded Sat 08:00. New rows D13–D16 / N1–N8 at the bottom are **candidates, not decisions**. *Friday's "tonight's target/floor" text is in git history.*

Sizes: **XS** ≈ 15 min · **S** ≤ 1 h · **M** 2–3 h · **L** half a day. "Done when" is the acceptance test — if a teammate can't tick it, it isn't done. PRs cost ~10 min each including everyone's pull — batch merges roughly hourly; after pulling with Godot open, **Scene → Reload Saved Scene**. Owner blank = unassigned. Node types in *italics* are the idiomatic Godot choice, not a requirement.

\* = **proposed split so every scene file has exactly one owner** (DESIGN §4.3). Swap names freely; keep one name per file.

## Friday night — template + one day (how it went)

| ID | Task | Owner | Needs | Size | Done when |
|---|---|---|---|---|---|
| **D5** | Decide fail details. **What exists:** triggers = sunset (every day) · pit (platforming_day) · panic max (day_panic); **presentation** = instant reload (WinConditions days) vs game-over card + Retry (DayManager day, `scenes/ui/game_over.tscn`). Pick one presentation, or keep both on purpose. *(Default if undecided: timer only, instant restart.)* *(reassessed)* | all | — | XS | written in DESIGN §2; one fail presentation, or both deliberately |
| **D11** | Decide: `R` = restart the day any time (softlock escape) · only on fail · unbound; `Esc`/`pause` = pause · nothing · drop it from the controls list (it also exits browser fullscreen); after the end card → intro · day 1 · "R to play again". **Today:** both actions are bound in project.godot and **nothing acts on them** (only the `main.tscn` placeholder echoes the name); the only *any-time* restart is Dev's F5 — debug-only, absent on the web, where you restart only through a fail (auto-reload on WinConditions days, the card's Retry on DayManager days); there is **no end card** — the reunion loads the `main.tscn` placeholder (N1). *(reassessed)* | all | N1 | XS | written in DESIGN §2; R/Esc wired or unbound |
| **J1** | Jam admin — re-read the jam page once: third-party-asset policy, AI/generative-art policy (ours: none — note the jam's exact wording), the submission form's required fields, credits rule, and the countdown's **deadline and timezone** vs our Sat 23:59. 5-line summary in Discord. **Done-when, split:** (1) timezone + jam-page time written beside DESIGN §2.3 Clock · (2) README "Where things are" link filled, or changed to "tracking in TASKS.md" · (3) Discord summary *(reassessed; nothing of it is in the repo yet)* | ? | — | XS | the three parts above |
| **E1** ◐ | Everyone: clone, open in Godot 4.7.1, run, download export templates (Editor → Manage Export Templates), one tiny PR merged | all | — | S | ✔ the PR loop — 15 PRs merged by all three (#1–#15). **Open:** Sean and Ben have export templates installed (Sean is on **Godot 4.7.2 / Windows**, Ben on `/Applications/Godot4.7.app` — see N5) |
| **T0** ✔ | API contract — signal names in `scripts/autoload/events.gd` and the methods everyone codes against. Landed as `condition_satisfied` / `day_completed` / `day_failed` / `sunset`, `Head.release()`, `Game.next_day()` / `restart_day()` / `go_to()` | Sean + Ben | — | XS | ✔ `events.gd` on `main` declares them |
| **T1** ✔ | Body controller — run + jump on flat ground; tunables as `@export`. *`scenes/body/body.tscn`: CharacterBody2D + `move_and_slide()`* (slopes cut Fri 18:45 — see T2) | Ben | — | M | ✔ body runs and jumps in the template scene |
| **T2** ✔ | Head — **starts already stuck** at its authored position; `release()` sends it off screen (translate + spin, scripted). Signals: `released`, `left_scene`. Joins group `"head"` so `Game` can find it without a hard path. *`RigidBody2D` held `freeze`d (set in `head.tscn`) and moved by script — scripted, not simulated (DESIGN §2.1)* **Scope cut Fri 18:45:** no roll-in, no entry/exit slopes, no `stuck` signal (it starts stuck). **Fri 19:15:** that roll off screen *is* the day's outro beat — no separate outro scene. **Both ratified by the team Fri 20:09.** | Sean | D2 ✔ | S | ✔ head sits still at open; `release()` sends it off screen and emits `left_scene`, every run |
| **T3** ✔ | Sun timer — `@export day_length`, sprite arcs across the top, emits `sunset` → **day fails → restart**. *`scenes/sun/sun.tscn`, `_process` on a sine arc* | Sean / Ben | D5 (default ok) | S | ✔ every day reacts to sunset (Events days restart via `WinConditions` → `day_failed`; DayManager days show the game-over card) and a won day ignores it — `tools/smoke_test.sh day_sunset` proves both, per day |
| **T4** ✔ | Keyed win-condition node — `WinCondition` with `@export key` (`body` / `mind`), `satisfy()`, `satisfied` signal, plus a `WinConditions` manager that finds every condition in the day and emits `Events.day_completed` when the last one lands. `Game` calls `head.release()` off that signal, not this node | Sean (built in PR #6; Ben was the Fri assignment) | D7 ✔ | M | ✔ two WinConditions satisfied in either order → head releases; one alone → nothing |
| **T5** ✔ | Day flow — `Game` autoload is the scene manager: `DAY_SCENES`, `start_days()`, `next_day()`, `restart_day()`, `go_to(i)` for testing; listens to `Events.day_completed`, releases the head, advances once `head.left_scene` fires; after the last day → reunion. Sole owner of `change_scene_to_file()` *(DayManager days still go through `Game.change_scene()` but bypass `DAY_SCENES`/`next_day()` — see D13)* | Sean | — | S–M | ✔ `DAY_SCENES` runs intro → day_template → transition_cage → day_panic → platforming_day → reunion (`tools/smoke_test.sh day_chain play_through` green); `next_day()` also advances an F6-opened scene correctly *(reassessed)* |
| **T6** ✔ | Fixed camera per scene — *Camera2D at scene centre, no follow* (one node in the template) | Ben | D3 ✔ | XS | ✔ scene is framed at 640×360 and doesn't move |
| **T7** ◐ | HUD — instruction text (`@export` per day) + win-key indicators that light up when satisfied (shown to start). *`CanvasLayer` + Labels/TextureRects; `Events.condition_satisfied` already fires on `WinConditions` days (template, panic) — DayManager days emit `WinConditionManager.condition_satisfied` locally instead, see D13* | ? (name someone) | T4 ✔ | S | ◐ instruction text exists on every day as a plain `Instruction` Label (not an `@export`) · win-lights **not started** — no HUD consumes `Events.condition_satisfied` yet (only `Sfx` listens, for the `need_met` cue). Decide: keep per-scene Labels vs a shared HUD scene *(reassessed)* |
| **T8** ◐ | **Day template scene** — `scenes/days/day_template.tscn`: flat ground / platforms · head at its stuck position · body spawn · Sun · HUD · WinConditions · camera. *(Slopes cut Fri 18:45 — see T2.)* Plus a 5-line "how to make a new day" note | Sean | T1–T7 | M | ✔ template (Background + sky drift, Camera, Floor, Body, Head, WinConditions, 2 goals, Sun, Instruction) · ✔ the note = [days/HOWTO.md](days/HOWTO.md) · ✗ HUD win-lights (T7). A new day = one new `.tscn` + one line in `Game.DAY_SCENES` (+ an optional bot plan) *(reassessed)* |
| **C2** ✔ | **Day 1** — the Bridge card (DESIGN §2.1 "(C2)"; the card's "goals vanish when collected" is not implemented — `spatial_goal.gd` pops the marker instead) = `scenes/days/platforming_day.tscn` + `.gd` (Sean; `extends DayManager`): three one-way steps → high goal drops the bridge → far goal across the gap; pit and sunset fail to the game-over card. In `Game.DAY_SCENES` (last — DayManager constraint, D13). *(reassessed — the old "build it as `day_01.tscn`" is superseded; `day_01.tscn` is a broken leftover, N2)* | Sean | T8 | M | ✔ bot-proven end to end; web build exported + booted in Chrome. Left: a human plays it in the browser (P2·0); D16 (the head blocks the path — intended?) |
| **A0** ◐ | Art direction kickoff — palette = Gooseberry Ghost (8) + bone shadow; body base = tbbk CC0 skeleton, recoloured, beheaded (decided); head = ours 16×16; sun = ours 16×16 (done); kiki/bouba sketch; which placeholders get swapped first. Tone: silly-spooky | Tucker | D6 ✔ | S | ✔ decisions + palette in `assets/palette/` (+ README) + `assets/sprites/README.md` · ✗ a one-screen style sheet (or declare the READMEs sufficient) · kiki/bouba sketch → A2 *(reassessed)* |
| **A1a** ✔ | Sprites into the scenes — body ✔ (PR #8), sun ✔ (PR #9), head ✔ (`head.tscn` is an `AnimatedSprite2D` on `head_frames.tres`: loose / imprisoned / wink); palette pass on intro / reunion / day_template / day_panic / platforming_day / transition ✔ | Tucker + scene owner | A0 | S | ✔ every scene renders from the palette (pixel-counted). **Kept rectangles:** SpatialGoal markers, floors, game-over card → A3 *(reassessed)* |
| **P1** | Restricted itch page with tonight's `web.zip` (`tools/export_web.sh`); secret URL in Discord. **Status:** export pipeline ✔ (`build/web.zip` from `night/all`, booted in Chrome) · itch upload + Sean/Ben as admins **unconfirmed** (not visible from the repo) · Sean's export needs 4.7.2 templates or a 4.7.1 install (N5) *(reassessed)* | Tucker | E1 | S | a teammate opens the URL on their machine and the template runs, **and Sean and Ben are added as admins on the itch project and have export templates installed (E1) — so anyone can export + upload from here on** |
| **E2** ◐ | Hard stop — agree the hour. The last person to merge runs `tools/export_web.sh` and uploads; everyone pushes; `main` runs | all | P1 | — | ◐ hard stop ~22:45 ✔ · `web.zip` built ✔ · **`main` does not have the night's work** — PR #16 + `night/*` are branches (N3) · itch upload ? (P1) *(reassessed)* |

## Saturday morning (09:00–13:00)

| ID | Task | Owner | Needs | Size | Done when |
|---|---|---|---|---|---|
| **E3** | Stand-up ≤ 15 min: play last night's *web* build together; assign one owner per day; decide **D8** (cut order) — **and D13–D16**. Agenda with options: [overnight-2026-08-22.md §4](overnight-2026-08-22.md) *(reassessed)* | all | P1 | S | names next to days 2–3; D8 + D13–D16 written in DESIGN §2 |
| **D8** | Decide the cut order — proposal at the bottom of this file (**rewritten against what exists**: there are no days 3–5 yet, so the order is about what ships of {day_template, transition_cage, day_panic, platforming_day, reunion, end card} plus which new days to build). The day part should follow which days teach the most, so it may need C1 first *(reassessed)* | all | E3 | XS | written in DESIGN §2 |
| **D12** | Decide the jump arc. `body.gd` is `jump_velocity = -300` / `gravity = 980` → **max rise 45.9 px**, hang 0.61 s, horizontal reach 92 px. The platforming day's steps sit 36 px apart, so step 1 floats only 36 px over the ground and the 42-px-tall body clips through its railing when walking underneath. Raising it needs ≥ 47 px of jump. Options: (a) raise all three steps 12 px + `jump_velocity = -320` as a per-scene override on that day's Body, (b) same but change `body.gd` globally — consistent feel, but it retunes every day, (c) leave heights and fill step 1 down to the ground so there is nothing to walk under (no physics change). Deferred Fri 22:31; step art already ends on a post either way | all | — | XS | written in DESIGN §2, and the platforming day matches it |
| **C1** ◐ | Day cards ×~5 — one idea each (`name · snag / what befalls the head · body need · verb for each · twist · length`; ideas in [days/brainstorm.md](days/brainstorm.md)). **1 card exists** (Bridge, DESIGN §2.1); day_panic and transition_cage were built without one; 4 loose ideas in brainstorm.md. Say where cards live (Google Doc vs `docs/days/`) *(reassessed)* | all | — | S | cards in the doc (panic's + ~3 more) |
| **C3a–b** ◐ | **Days 2 and 3** — one file, one owner each, on the template; rectangles first, art after. **C3a ✔ = `day_panic.tscn`** (Ben's scene, PR #11; the caged head / calm-to-zero rework is Tucker's on PR #16 — N3): caged head, PanicCounter — calm it from 15 to 0 to win, 30 fails; **mind need only** (D15). **C3b not started.** *(reassessed)* | Ben ✔ / Sean\* | T8 ✔, C1 (C2 as the worked example) | M each | each day runs end to end in the web build |
| **C3c–d** | **Days 4 and 5** — start **only after Days 2–3 run in the web build** (expect ~13:00, not 11:00) | whoever's day ran first | C3a–b | M each | same |
| **C4** ◐ | Intro beat — head falls off and starts rolling down; leads into day 1. **Today:** `scenes/intro/` is a playable chase (head runs right, you follow) → `Game.start_days()` ✔. The **pop-off beat** (whole skeleton → intrusive thoughts → head pops → chase) is a TODO in `intro.gd` and needs A2 — or becomes a text card (D8) *(reassessed)* | ? | T1 ✔, T2 ✔, A1 ✔, A2 | M | from "Run" to day 1 without (or with) a hand on the keyboard |
| **A1** ✔ | Body + head sprites — shipped as `body_walk/idle/throw.png` + `head_front/side/keyed.png` + SpriteFrames `.tres`, wired in `body.tscn` / `head.tscn` (colliders unchanged, feel unchanged); roll frames dropped — `head.gd` spins the sprite. Sources in `assets/sprites/src/` *(reassessed — old file names superseded)* | Tucker + scene owners | A0, T1/T2 stable | S–M | ✔ template runs with sprites; kept rectangles listed under A1a |
| **A2** | Kiki / bouba intrusive-thought shapes (for the intro; mid-day use is §3.5 / X3) | Tucker | A0 | S | shapes in `assets/sprites/` |
| **S1** ◐ | SFX pass — jump, land, need satisfied, head released, day fail, sunset (jsfxr/ChipTone or hand-made). **Wiring is done** (`Sfx` autoload, by signal — no per-day work; `night/sfx`). **Files: 0/16** — record into `assets/audio/sfx/<cue>.wav` per [assets/audio/README.md](../assets/audio/README.md). Plus 3 one-line calls: `bridge_drop` (Sean's `_drop_bridge()`), `cage`/`thud` (transition) *(reassessed)* | Tucker / Ben | S0 ✔ | S–M | every event in day 1 makes a sound on web |
| **P1r** | Export + upload after every merged day and after the sprite swap — whoever merged (anyone can, per P1). `tools/smoke_test.sh --web` first. **Note:** the current local `web.zip` predates the 07:41 review fixes — rebuild after N3 *(reassessed)* | whoever merged | P1 | XS each | the itch URL always runs the latest `main` |

## Saturday afternoon (13:00–19:00)

| ID | Task | Owner | Needs | Size | Done when |
|---|---|---|---|---|---|
| **E4** | 13:00 **scope check** — apply the cut order written in DESIGN §2 (D8); if it isn't written yet, the first 10 min of E4 is writing it | all | D8 | XS | §2 has a cut order and the tasks below reflect it — applied, not re-debated |
| **C5** | Reunion beat — after the last day; head and body reunite (look: §3.6; brainstorm: faces camera, walks into the screen). Or a text card (D8) | ? | C3 (at least the last day), A1 | M | last day → reunion → end card, in the web build |
| **A3** | Per-day props, sun, HUD art — swap behind stable code | Tucker | A1, C3 | M | no coloured rectangles left in shipped days (kept ones listed in the PR) |
| **S2** | Per-day music — only if days exist and time allows, after E4 | Tucker / Ben | C3, E4 | M–L | each shipped day has a loop; web audio starts after first input |
| **P2·0** | 17:00 mini-playtest of the current web build (round 0) → **P5 decisions land here, per day**. Seed the fix list from [overnight-2026-08-22.md §3–4](overnight-2026-08-22.md) (end card, R/Esc, head blocks the path, step-1 clip) *(reassessed)* | all | P1r | S | a list of what to fix before freeze |
| **P5** | Per day: keep or drop the `Instruction` Label (a deliberate yes/no); win-lights only if T7 lands *(reassessed)* | day owners | P2·0 | S | each day's text (and lights) is a deliberate yes/no |
| **P3** ◐ | Itch page *(◐: credits material in `assets/sprites/CREDITS.md` + `assets/palette/README.md` (add the palette credit to CREDITS), embed settings + controls in README; cover / shots / GIF / page text not started)* — cover image (630×500), text, controls (keyboard; gamepad only if tested, §3.10), 2 screenshots, a GIF, one line on the theme, **credits** (Tucker / Sean / Ben; skeleton body base: tbbk — opengameart.org/content/pixel-art-skeleton, CC0; any third-party font, SFX or tool with its licence; answer any AI-disclosure field the jam has) — embed settings per README → "Exporting to itch.io". Text now; shots + GIF from the 19:00 build | ? | A3, J1 | S | a teammate opens the Restricted URL and ticks every item in this row; nothing from J1's summary is missing |
| **E5** | **Feature freeze 19:00** — nothing new after this, only fixes | all | — | — | last feature PR merged |

## Saturday evening (19:00–22:30) — ship

| ID | Task | Owner | Needs | Size | Done when |
|---|---|---|---|---|---|
| **P2** | 19:00–21:30 Export → itch; everyone plays every day on the *web* build in two browsers (Chrome + Firefox; Safari if handy); **showstoppers only** = crash · softlock · can't progress · can't start on web · controls don't respond until a click (that one's expected — see README). **Pre-seeded:** no end card (N1), no R/Esc (D11), Firefox/Safari untested *(reassessed)* | all | E5 | M | a written list of zero open showstoppers |
| **P4** | 21:30–22:30 Final export, upload, **itch page Restricted → Public**, page check, **Submit by 22:30** — **name the owner this morning** *(reassessed)* | ? (name them) | P2, J1 | S | submission confirmed on the jam page; opened from the jam's entries list in a private/incognito window (logged out — not via the secret URL) the game runs; screenshot in Discord |

## Stretch — only after P2 has zero showstoppers

| ID | Task | Needs |
|---|---|---|
| **X1** | Each day = a *mental* task + a *physical* task | C3 |
| **X2** | End scene: battle against the intrusive thoughts | C5, A2 |
| **X3** | Intrusive thoughts as a mid-day hazard (§3.5) | A2, C3 |
| **X4** | Title / press-any-key screen (also unlocks web audio — today the intro plays **hands-free** and the first keypress is in day_template, so the intro is silent on the web; a title screen would move the audio unlock earlier as well as add the beat). Pairs with the end card (N1) | — |

## Cut order — proposed, not decided (D8) — *rewritten Sat 08:00 against what exists*
Nothing in DESIGN §2 ranks these yet (§4.5: *pick it while you're calm; write it in §2 once chosen*). Decide it at E3 once the day cards exist, before E4. **What exists to rank** (in the run today): `day_template` (the bare template, plays first — D14) · `transition_cage` · `day_panic` (mind-only — D15) · `platforming_day` (day 1) · `reunion` · *no end card* (N1). **Proposal to react to, cheapest-to-lose first:** new days 4–5 (not built) → day 3 (not built) → per-day music (S2; `day.ogg` alone is fine) → transition forks beyond the cage one → the intro pop-off beat becomes "the chase as is" (C4) → `day_template` dropped from the run (D14) → reunion stays, but its end card is a title card, not prose (§2 Words). **Never cut** (traces to §2 — min ship = 1 day, web target, template first): the template, day 1 (`platforming_day`), a web build that runs, the itch page, **an end card of some kind** (today the game ends on a debug screen).

## New since Friday — Sat 08:00 reassessment (candidates, not decisions)

Decisions the code has forced; work the repo shows is needed. IDs continue the numbering. Owners blank = unassigned.

| ID | Task | Owner | Needs | Size | Done when |
|---|---|---|---|---|---|
| **D13** | **One day-flow system or two?** `WinConditions`+`Events`+`Game` (template, day_panic) vs `WinConditionManager`+`DayManager` (platforming_day). Until decided: pick one per day, don't mix; a DayManager day must be **last** in `DAY_SCENES` (`game.gd` comment, `day_lint` enforces). Options + trade-offs: [days/HOWTO.md §3](days/HOWTO.md), [overnight §4.3](overnight-2026-08-22.md) | all | — | XS (decide) · S–M (unify) | written in DESIGN §2; days 2–5 built on the chosen one |
| **D14** | Does `day_template.tscn` ship as day 1 of the run? It is `DAY_SCENES[0]` today ("Template day: touch both boxes."). Keep as a tutorial beat, or drop it from the list | all | — | XS | written in DESIGN §2; `DAY_SCENES` matches |
| **D15** | `day_panic` has a **mind need only** (DESIGN §2.1 wants body + mind; `day_lint` warns). Add a body need, or record that one need is fine for that day | all, Ben | — | XS · S | written in DESIGN §2; scene matches |
| **D16** | In `platforming_day` the head at (450,306) is a **solid 28×28 block between the bridge and the far goal** — the player must jump over it (the bot does). Intended? If not: move the head past the goal or clear its collision layer | Sean | — | XS | answered; scene matches |
| **N1** | **End card after the reunion** — `reunion.gd`'s `next_scene` loads `scenes/main.tscn` (the old "template OK — press a key" input test). Decide the look (DESIGN §3.6; §2 Words → a title card, no prose; "R to play again" → D11) and build it; point `next_scene` at it (the bot already follows `next_scene` — `play_through` needs no change) | ? | D11 | S | the run ends on the card; `play_through` reaches it |
| **N2** | `scenes/days/day_01.tscn` — delete or restore (references a missing `day_01.gd`; superseded by platforming_day; parked in `tools/smoke/known_broken.txt`) | Sean | — | XS | file gone or loads; `known_broken.txt` empty |
| **N3** | **Merge the night's work:** PR #16 (`tucker/palette-pass`) + `night/base → transition → daykit → sfx → anim` (or `night/all`, identical content) into `main`; `tools/smoke_test.sh --web`; upload (P1r). Order + conflict notes: [overnight §1](overnight-2026-08-22.md) | Tucker | review ✔ | S | `main` == `night/all`; itch runs it |
| **N4** | Dead code / housekeeping before the repo is public: `Events.sunset` never emitted (emit or delete) · `intro.gd` `@export next_scene` never read (remove) · the comment at `panic_label.gd:7` · exclude `scenes/reunion/preview/` from the web export (or leave, it's harmless) | anyone | — | XS | grep clean |
| **N5** | Multi-machine: CLAUDE.md §4 / README assume `/Applications/Godot.app`; Sean is on **Windows Godot 4.7.2**, Ben on `Godot4.7.app`. `tools/*.sh` honour `GODOT=/path` — document it; confirm 4.7.2 opens the 4.7.1 project and exports (P1) | Sean, Ben | — | XS | both can run `tools/smoke_test.sh` and `tools/export_web.sh` |
| **N6** | Transition forks — one per remaining day that wants the head-rolls-downhill beat (recipe: `scenes/transition/transition.gd` header; cage fork is the example); scripted vs playable (overnight §4.9) | day owners | TR ✔, the day | S each | fork in `DAY_SCENES` before its day; bot hands off |
| **N7** | SFX hooks still to call: `Sfx.play(&"bridge_drop")` in `platforming_day.gd _drop_bridge()`, `&"cage"`/`&"thud"` in the transition; optional `step` on walk-frame change | Sean / Tucker | S0 ✔ | XS | cues fire (audio suite can assert) |
| **N8** | `sky_drift.gd` on `platforming_day`'s Background (only template/panic have it) — if the team wants the dusk drift on every day; also DESIGN: confirm off-palette drift is OK with all three | Sean | anim | XS | consistent across days |
