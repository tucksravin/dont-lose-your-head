# Sprites — how to wire them in (owners of `body.tscn` / `head.tscn`)

All art is 1× pixel art on a 32×32 (body) / 16×16 (head) canvas. **Render at integer scale 2** — that matches the 24×32 / 28×28 placeholders the movement was tuned against (see `scenes/reunion/preview/sprites_preview.tscn`, F6).

**Body** (`scenes/body/body.tscn`, floor-origin — feet at y = 0):
1. Replace the `Visual` ColorRect with an **`AnimatedSprite2D`**: `sprite_frames = res://assets/sprites/body_frames.tres`, `animation = "walk"` (also has `idle`, `throw`), `centered = false`, `offset = Vector2(-16, -32)`, `scale = Vector2(2, 2)`. Feet land on the origin; the headless body is 13×19 px at 1× (26×38 on screen).
2. In `body.gd`: `$AnimatedSprite2D.flip_h = velocity.x < 0.0` when moving; play `walk` when `abs(velocity.x) > 1`, else `idle` (add `if` around `play()` so it doesn't restart every frame).
3. Collider: the body is now 26×38 on screen; a `RectangleShape2D` ≈ `Vector2(20, 38)` at `position (0, -19)` hugs it (the old 24×32 was for the rectangle). Tune to taste.

**Head** (`scenes/head/head.tscn`) — **wired**: `Visual` is an `AnimatedSprite2D` on `head_frames.tres` (`loose` = the plain head, `imprisoned` = the 4-frame cage loop from `head_keyed.png`, `wink`). A day cages its head with `caged = true` on the Head instance, and `Head.set_agitation(x)` scales how fast the cage loop runs — `day_panic` drives that from the panic meter. Geometry is unchanged from the old `Sprite2D`: `scale = (2,2)`, centered (so the release spin in `head.gd` rotates about the skull's middle). The intro uses `head_side.png` instead, because there the head is running to the right. Scripted, not simulated: a frozen `RigidBody2D` moved by code, DESIGN §2.1.
1. Replace `Visual` with a **`Sprite2D`**: `texture = res://assets/sprites/head_front.png` (or `head_side.png`), `scale = Vector2(2, 2)` (centered). 16×16 → 32×32 on screen.
2. The collider shape no longer matters for the roll-off (it's scripted) — keep the rectangle.
3. Rotation: spin the sprite from the release script (`rotation += spin_speed * delta`; jaggies at 2× are mild) or keep it upright and swap frames by angle later (`head_roll.png` isn't made yet).

**Sun** (`scenes/sun/sun.tscn`) — wired: `Visual` is a `Sprite2D`, `texture = res://assets/sprites/sun.png` (Tucker's 16×16, palette #f1ffaf disc / #b2f167 rays), `scale = Vector2(2, 2)`, centered so `Sun.position` is still the arc centre. Source `src/sun.aseprite`.

Texture filtering is Nearest project-wide; nothing to set per sprite. Sources are in `src/` (Aseprite); credits in `CREDITS.md`.

**Palette:** [`assets/palette/`](../palette/README.md) — the `.gpl` to load in Aseprite, and the swatch strip. All shipped art comes from it.

**Where the sprites are wired (Fri 20:30):** body → `scenes/body/body.tscn` · head → `scenes/head/head.tscn` (front) and `scenes/intro/intro.tscn`'s `HeadBlob` (side) · sun → `scenes/sun/sun.tscn`. Backgrounds and floors in `intro` / `day_template` / `reunion` are palette flat colours — all three skies are `#988277` (a background must never be `#201c02`: that is the sprites' own outline colour, and the silhouettes vanish into it); the two `SpatialGoal` markers are still rectangles, coloured per need from `spatial_goal.gd` (`body_color` / `mind_color`) because there's no art for them yet.

**Wired since (Fri night):** `head_keyed.png` → `assets/sprites/head_frames.tres` (`imprisoned` = f5–8, `wink` = f4; f1–3 unused so far) · `bridge.png` → the bridge and the three steps in `platforming_day.tscn`. Notes on each:
- `head_keyed.png` — 128×16, **8 frames of 16×16**, with Aseprite tags: **`look left`** (f1) · **`center`** (f2) · **`look right`** (f3) · **`wink`** (f4) · **`imprisoned`** (f5–8, a 4-frame loop at 100 ms/frame — the head behind bars). `head_frames.tres` now exposes `look_left` / `look_right` (mirror day); `Head.look(dir)` plays them.
- `bridge.png` — a fence/railing, **24×8**, recoloured by Tucker (posts `#45381c`, rope + deck line `#645543`). **The shipped PNG is deliberately 24 wide, not the 26 the art trims to**: the source has a closing post at columns 24–25, and tiling a 26-wide texture butts that post against the next tile's opening post, giving a 4 px double post at every seam. Export it with `aseprite -b src/bridge.aseprite --crop 4,8,24,8 --save-as bridge.png` — NOT `--trim` — so the repeat period stays 24 and posts stay 6 px apart. Used tiled (`texture_repeat` + `region_rect`) for the bridge and the three steps in `platforming_day`; a region width of `24n + 2` ends on a whole closing post.
- `head_side.png` was re-exported from Tucker's updated source (12 px changed). The source has 3 frames but they are pixel-identical, so only frame 1 ships — `intro.tscn` uses it as a single texture, not a sheet.

**Kikis (Sat, for Sean):** `kikis.png` — 336×16, **21 frames of 16×16**, the intrusive-thought shapes, in the violet ramp (`#5e2d8c` / `#8a4fb5` / `#c79df2`) only. Source `src/kikis.aseprite` (Tucker's Sat 11:42 version — **no tags in the file**; the names below are ours): **`lil_kiki`** = f0–8, the small one, ~4×4 px; **`big_kiki`** = f9–20, the big one, now a 12-frame spinning/morphing loop. Drop-in: **`kiki_frames.tres`** (SpriteFrames) → an `AnimatedSprite2D` with `sprite_frames = res://assets/sprites/kiki_frames.tres`, `animation = "lil_kiki"` (9 frames, 10 fps, loops — the file's 100 ms/frame) or `"big_kiki"` (f9–20, 12 frames, 10 fps, loops; *the name is ours — tag it in Aseprite if you want it to live in the source*). Render at 2× like everything else (→ 32×32 cells; the lil kiki is ~8 px on screen). No collision shape ships with it — what a kiki *does* is a day decision.

**Glasses (Sat, Tucker):** two things.
- `glasses.png` — a standalone 16×16 glasses sprite (green `#25c04b` only), **bottom-aligned in its cell** (rows 11–15), so with a bottom-origin it sits on the ground like a pickup. Source `src/glasses.aseprite`. Not wired anywhere — what the glasses *are* in a day is a day decision.
- `head_keyed_glasses.png` — 192×16, **12 frames**: Tucker's re-laid-out head sheet with glasses variants (source `src/head_keyed_glasses.aseprite`, **no Aseprite tags** — mapped pixel-for-pixel against the old sheet: f0 look-left · **f1 look-left + glasses** · **f2 centre + glasses** · f3 centre · **f4 look-right + glasses** · f5 look-right · f6 wink · **f7 wink + glasses** · **f8–11 caged + glasses** (the caged loop here runs caged1-2-1-4 rather than 1-2-3-4; each glasses frame = the plain frame + the same 24 green px). The shipped `head_keyed.png` (8 frames, tagged) is **unchanged** and still backs `loose`/`imprisoned`/`wink`; the new sheet is exposed as extra animations in `head_frames.tres`: **`glasses`** (f2, centre) · `glasses_look_left` (f1) · `glasses_look_right` (f4) · `wink_glasses` (f7) · **`imprisoned_glasses`** (f8–11, 10 fps). So nothing changes until a scene asks for it: to put glasses on a day's head, set its `Visual.animation = "glasses"` (or play `imprisoned_glasses` when caged). If the team wants glasses *everywhere*, that's a one-line swap in `head.tscn` / `head.gd` — say so and we'll do it.

**Barbell (Sat, Tucker — for the workout day):** `barbell.png` — 128×32, **4 frames of 32×32**, palette-only (`#25c04b` / `#b2f167` plates + bar, `#45381c` / `#645543` / `#f1ffaf` body). Source `src/barbell.aseprite`, **tags kept as animation names** in **`barbell_frames.tres`**: **`alone`** (f0 — the barbell on the floor, bottom-aligned in the cell, 32×11 px) · **`with_body`** (f1 — the body standing behind it, `with body` in Aseprite) · **`lifting`** (f2–3 — squat → overhead, 10 fps loop). Frames 1–3 *include the skeleton*, so they are a stand-in for body + bar together, not an overlay. Not wired: `scenes/gameplay/barbell.tscn` (Sean, #27) still draws three ColorRects. Two ways to wire, Sean's call: (a) smallest — replace `LeftWeight`/`RightWeight`/`Bar` with one `AnimatedSprite2D` at 2× playing `alone` (64×22 on screen vs the 80×20 rects; nudge `carry_offset` to taste) and keep the pump tween as is; (b) fuller — on pickup hide the body's `Visual` and play `with_body` → `lifting` on the mash instead of the tween. *Note: in these frames the body's outline is `#45381c` (dark brown), not the body sprite's `#201c02` — intentional or not, Tucker's to say.*

**Scene palette status (Fri 21:35):** `intro` · `day_template` · `reunion` · `platforming_day` all render entirely from the palette (verified by counting every pixel of a rendered frame). The only off-palette pixels anywhere are the antialiased edges of HUD/label text — Godot renders TTF text with antialiasing, so that is expected until we ship a bitmap font.

Still un-palettised: `game_over.tscn`'s dim/title (and `day_01.tscn`, which cannot open). `day_panic` and the transition were palettised Fri night.
